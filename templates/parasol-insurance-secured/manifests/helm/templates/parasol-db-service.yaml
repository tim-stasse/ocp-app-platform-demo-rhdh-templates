apiVersion: v1
kind: Service
metadata:
  name: parasol-db
  namespace: ${{ values.namespace }}
  labels:
    app: parasol-db
spec:
  ports:
    - name: tcp-postgresql
      protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    app: parasol-db
